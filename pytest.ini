[pytest]
testpaths = tests
markers =
    unit: pure-logic unit tests (no network, deterministic)
    integration: sandboxed integration tests (real broker/redis, mock cloud)
    live: black-box smoke against the running service (may emit Telegram alerts)
    manual: physical E2E runbook (camera/RFID/door — run by a human)
addopts = -ra